# workspace:
#   base: /src/drone-demo
#   path: .

pipeline:
  build:
     image: golang:alpine
     # pull: true
     commands:
       - pwd
       - ls
       - CGO_ENABLED=0 GOOS=linux go build -a -installsuffix cgo -o demo63 .
       - ./demo63